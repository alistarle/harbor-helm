apiVersion: v1
kind: Service
metadata:
  name: registry-frontend-service
  namespace: {{ .Release.Namespace | quote }}
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
